describe('ShowUser', () => {
  it('should be able to return a user');
  it('should return error if authenticated user is not root or admin');
  it('should return the data of authenticated user if his type is general');
});
